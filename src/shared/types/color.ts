// src/shared/types/color.ts

/**
 * A color, in the form every renderer, theme and piece tint passes around:
 * four floats 0-1, red-green-blue-alpha.
 */
export type Color = [number, number, number, number];
